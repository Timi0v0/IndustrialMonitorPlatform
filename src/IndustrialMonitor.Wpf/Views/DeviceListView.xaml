<UserControl
    x:Class="IndustrialMonitor.Wpf.Views.DeviceListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:IndustrialMonitor.Wpf.ViewModels"
    d:DataContext="{d:DesignInstance vm:DeviceListViewModel}"
    mc:Ignorable="d">
    <Grid Margin="24" Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Style="{StaticResource PageTitleText}" Text="设备管理" />
                <TextBlock Style="{StaticResource PageSubtitleText}" Text="维护采集设备、通信协议和运行状态" />
            </StackPanel>

            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Margin="0,0,8,0"
                    Command="{Binding RefreshCommand}"
                    Content="刷新"
                    Style="{StaticResource ButtonSecondary}" />
                <Button
                    Command="{Binding AddDeviceCommand}"
                    Content="新增设备"
                    Style="{StaticResource ButtonPrimary}" />
            </StackPanel>
        </Grid>

        <Border
            Grid.Row="1"
            Margin="0,0,0,12"
            Padding="14"
            Style="{StaticResource SurfaceCard}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <hc:SearchBar
                    Grid.Column="0"
                    Width="360"
                    HorizontalAlignment="Left"
                    hc:InfoElement.Placeholder="搜索设备编号或名称..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Foreground="{StaticResource TextMutedBrush}"
                    Text="{Binding TotalCount, StringFormat=当前共 {0} 台设备}" />
            </Grid>
        </Border>

        <Border
            Grid.Row="2"
            Padding="0"
            Style="{StaticResource SurfaceCard}">
            <DataGrid
                AutoGenerateColumns="False"
                FontSize="13"
                IsReadOnly="True"
                ItemsSource="{Binding Devices}"
                SelectedItem="{Binding SelectedDevice}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding DeviceCode}"
                        Header="设备编码" />
                    <DataGridTextColumn
                        Width="1.4*"
                        Binding="{Binding DeviceName}"
                        Header="设备名称" />
                    <DataGridTextColumn
                        Width="1.1*"
                        Binding="{Binding DeviceType}"
                        Header="设备类型" />
                    <DataGridTextColumn
                        Width="90"
                        Binding="{Binding ProtocolType}"
                        Header="协议" />
                    <DataGridTextColumn
                        Width="140"
                        Binding="{Binding IpAddress}"
                        Header="IP 地址" />
                    <DataGridTextColumn
                        Width="80"
                        Binding="{Binding Port}"
                        Header="端口" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding CollectInterval}"
                        Header="采集间隔(ms)" />
                    <DataGridTemplateColumn Width="90" Header="状态">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border
                                    Padding="9,3"
                                    HorizontalAlignment="Left"
                                    CornerRadius="10">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#DCFCE7" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                    <Setter Property="Background" Value="#FEE2E2" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="12" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="启用" />
                                                <Setter Property="Foreground" Value="#166534" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                        <Setter Property="Text" Value="停用" />
                                                        <Setter Property="Foreground" Value="#991B1B" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="150" Header="操作">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Button
                                        Margin="0,0,6,0"
                                        Command="{Binding DataContext.EditDeviceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="编辑"
                                        Style="{StaticResource ButtonSmallSecondary}" />
                                    <Button
                                        Command="{Binding DataContext.DeleteDeviceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="删除"
                                        Style="{StaticResource ButtonDangerSmall}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Foreground="{StaticResource TextMutedBrush}"
                Text="{Binding TotalCount, StringFormat=共 {0} 条记录}" />
            <TextBlock
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Foreground="{StaticResource TextMutedBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="数据已就绪" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Text" Value="正在加载数据..." />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>
